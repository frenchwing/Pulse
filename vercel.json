{
  "buildCommand": "pnpm --filter @workspace/playmate run build",
  "outputDirectory": "artifacts/playmate/dist/public",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
